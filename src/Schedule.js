import React from 'react';
import Section from './Section';

const Schedule = () => {
  return (
    <Section id="schedule" title="Event Schedule">
      <p>
        The full schedule, including keynote speakers, workshop times, mentoring sessions, project submission deadlines, 
        and the final judging process, will be detailed here.
      </p>
    </Section>
  );
};

export default Schedule;
